<h2><%= @post.title %></h2>
<% if @post.photos.nil? %>
  <% cover_photo = @post.photos[0] %>
  <%= cl_image_tag cover_photo.key, width: 400, height: 300, crop: :fill %>
<% end  %>
<%= link_to "Back", :back %>
