<div class="section">
  <div class="post-card">
    <h1><%= post.title %></h1>
    <p><%= post.text %></p>
    <% if post.photos.attached? %>
      <%= cl_image_tag(post.photos[0].key , width: 400, height: 600, crop: :fill) %>
    <% end %>
  </div>
</div>
